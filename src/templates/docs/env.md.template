# Environment Variables

This document describes all environment variables used in {{ PROJECT_NAME }}.

## Overview

Environment variables are configured in the `.env` file in the project root. This file is not committed to version control for security reasons.

## Project Configuration

| Variable | Description | Default |
|----------|-------------|---------|
| `PROJECT_NAME` | Project name | `{{ PROJECT_NAME }}` |
| `DOCKER_NETWORK` | Docker network name | `{{ DOCKER_NETWORK }}` |

## Backend Configuration

{% if BACKEND_STACK %}
| Variable | Description | Default |
|----------|-------------|---------|
| `BACKEND_PORT` | Backend service port | `{{ BACKEND_PORT }}` |
| `BACKEND_STACK` | Backend framework | `{{ BACKEND_STACK }}` |
| `BACKEND_LANGUAGE` | Backend language | `{{ BACKEND_LANGUAGE }}` |

### Backend Environment Variables

The backend service uses the following environment variables:

- `BACKEND_PORT`: Port on which the backend API listens
- `BACKEND_STACK`: Framework being used ({{ BACKEND_STACK }})
{% else %}
Backend is not configured for this project.
{% endif %}

## Frontend Configuration

{% if FRONTEND_STACK %}
| Variable | Description | Default |
|----------|-------------|---------|
| `FRONTEND_PORT` | Frontend service port | `{{ FRONTEND_PORT }}` |
| `FRONTEND_STACK` | Frontend framework | `{{ FRONTEND_STACK }}` |

### Frontend Environment Variables

The frontend service uses the following environment variables:

- `FRONTEND_PORT`: Port on which the frontend application runs
- `FRONTEND_STACK`: Framework being used ({{ FRONTEND_STACK }})
{% else %}
Frontend is not configured for this project.
{% endif %}

## Database Configuration

{% if DATABASE_STACK %}
| Variable | Description | Default |
|----------|-------------|---------|
| `DATABASE_STACK` | Database engine | `{{ DATABASE_STACK }}` |
| `DATABASE_PORT` | Database port | `{{ DATABASE_PORT }}` |
| `DATABASE_NAME` | Database name | `{{ DATABASE_NAME }}` |
| `DATABASE_USER` | Database user | `{{ DATABASE_USER }}` |
| `DATABASE_PASSWORD` | Database password | `*` (set in .env) |
| `DATABASE_HOST` | Database host | `database` (Docker) or `localhost` (local) |

### Database Connection Strings

{% if DATABASE_STACK == "postgres" %}
**PostgreSQL:**
```
postgresql://{{ DATABASE_USER }}:{{ DATABASE_PASSWORD }}@{{ DATABASE_HOST }}:{{ DATABASE_PORT }}/{{ DATABASE_NAME }}
```

**Docker Compose:**
```
postgresql://{{ DATABASE_USER }}:{{ DATABASE_PASSWORD }}@database:5432/{{ DATABASE_NAME }}
```

**Local Development:**
```
postgresql://{{ DATABASE_USER }}:{{ DATABASE_PASSWORD }}@localhost:{{ DATABASE_PORT }}/{{ DATABASE_NAME }}
```

{% elif DATABASE_STACK == "mongo" %}
**MongoDB:**
```
mongodb://{{ DATABASE_USER }}:{{ DATABASE_PASSWORD }}@{{ DATABASE_HOST }}:{{ DATABASE_PORT }}/{{ DATABASE_NAME }}
```

**Docker Compose:**
```
mongodb://{{ DATABASE_USER }}:{{ DATABASE_PASSWORD }}@database:27017/{{ DATABASE_NAME }}
```

**Local Development:**
```
mongodb://{{ DATABASE_USER }}:{{ DATABASE_PASSWORD }}@localhost:{{ DATABASE_PORT }}/{{ DATABASE_NAME }}
```

Or for auth-less setup:
```
mongodb://localhost:{{ DATABASE_PORT }}/{{ DATABASE_NAME }}
```

{% elif DATABASE_STACK == "redis" %}
**Redis:**
```
redis://{{ DATABASE_HOST }}:{{ DATABASE_PORT }}
```

**Docker Compose:**
```
redis://database:6379
```

**Local Development:**
```
redis://localhost:{{ DATABASE_PORT }}
```
{% endif %}

### Security Notes

- **Never commit** the `.env` file to version control
- Use strong passwords in production
- Rotate database passwords regularly
- Use environment-specific `.env` files (`.env.development`, `.env.production`)
{% else %}
No database is configured for this project.
{% endif %}

## Docker Configuration

| Variable | Description |
|----------|-------------|
| `DOCKER_NETWORK` | Docker network name for service communication |

## Setting Environment Variables

### Docker Compose

Variables in `.env` are automatically loaded by Docker Compose. You can also override them:

```bash
# In docker-compose.yml
environment:
  - BACKEND_PORT=8000
```

### Local Development

**Linux/macOS:**
```bash
export BACKEND_PORT=8000
```

**Windows (PowerShell):**
```powershell
$env:BACKEND_PORT=8000
```

**Windows (CMD):**
```cmd
set BACKEND_PORT=8000
```

### Using .env File

Create a `.env` file in the project root:

```bash
PROJECT_NAME={{ PROJECT_NAME }}
{% if BACKEND_PORT %}
BACKEND_PORT={{ BACKEND_PORT }}
{% endif %}
{% if FRONTEND_PORT %}
FRONTEND_PORT={{ FRONTEND_PORT }}
{% endif %}
{% if DATABASE_STACK %}
DATABASE_STACK={{ DATABASE_STACK }}
DATABASE_PORT={{ DATABASE_PORT }}
DATABASE_NAME={{ DATABASE_NAME }}
DATABASE_USER={{ DATABASE_USER }}
DATABASE_PASSWORD=your_secure_password_here
{% endif %}
```

## Environment-Specific Configuration

For different environments, use separate `.env` files:

- `.env.development` - Development environment
- `.env.staging` - Staging environment
- `.env.production` - Production environment

Load them explicitly:
```bash
docker compose --env-file .env.production up
```


